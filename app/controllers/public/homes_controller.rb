class Public::HomesController < ApplicationController
  def top
    #@user = User.find(current_user.id) if user_signed_in?
  end

  def contact
    #@user = User.find(current_user.id) if user_signed_in?
  end
end
