<%= render "layouts/user_header" %>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h4 class="headline mt-5 ml-5">いいねしたコメント</h4>
        <div class="row mt-3">
        <% @liked_comments.each do |comment| %>
          <%= render 'public/users/comment', comment: comment %>
        <% end %>
      </div>
      <div class="d-flex justify-content-center">
        <%#= paginate @liked_posts, theme: 'bootstrap-5' %>
      </div>
    </div>
  </div>
</div>